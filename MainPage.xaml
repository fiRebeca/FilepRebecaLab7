<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FilepRebecaLab7.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Frame BorderColor="Black" CornerRadius="10" Padding="10" BackgroundColor="LightGray" HasShadow="True">
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Image Source="edelweiss.png" HeightRequest="100" WidthRequest="100" />
                    <Label Text="Filep Rebeca" FontSize="18" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />
            <Label
                Text="We're going to help you find an efficient way of shopping"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level2"/>
            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
